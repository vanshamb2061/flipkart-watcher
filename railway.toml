[build]
builder = "NIXPACKS"

[deploy]
cronSchedule = "*/10 * * * *"   # runs every 10 minutes
startCommand = "python scraper.py"
restartPolicyType = "NEVER"     # cron job — don't keep alive between runs
